[package]
name = "upgrade-fixture"
version = "0.1.0"
edition = "2021"
publish = false
description = "Test-only Soroban contract used as the target of a registry upgrade. Not part of the deployed system."

[lib]
crate-type = ["lib", "cdylib"]

[dependencies]
soroban-sdk = { workspace = true }
